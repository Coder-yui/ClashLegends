# 水晶动画与音频

2026-09-13：按原版 AnimationGraphData 恢复出生前保持 → 出生 → 循环待机，以及独立死亡/破碎表面。源表快照见 docs/archive/2026-09-13/research/nexus_animation_source.txt。

- 原表 Idle1 Sequencer 含两个匿名原子节点和 Idle1_Base；结合导入动作识别为 Nexus_spawn_hold → Nexus_spawn → Idle1_Base。
- 使用现有 GLB 原片长度：出生约 5.1667 秒、死亡约 8 秒，取消此前 2.5 / 4 秒强制压缩。初始仅采样出生前保持姿态，进入游戏立即播放上升，不停留；不将 mTickDuration 未完全核实的帧时钟字段直接宣称为原版状态时长。此处恢复的是原图结构与骨骼遮罩，不声称完整复刻原版帧调度。
- 原 Base 遮罩为骨骼表末尾 base/crystal，Destroyed 为其余骨骼；运行实例复制动画库后过滤，不修改共享 GLB。
- 当前两方 GLB 的出生/待机动作相同，对应 Order 源图过渡为零；出生转待机恢复零混合，保留跨帧剩余时间。其他切换同样为零混合。Chaos 原表含 IdleIn，但当前 GLB 不包含它，当前未接入原生 Chaos 的 IdleIn。出生节点同时列出 Audio_Spawn 与 Audio_Idle，运行时同时启动出生声与运转循环，出生尾音自然结束；运转 WAV 使用音频层循环。死亡/重置同时清理两个声源。
- 死亡切换 Destroyed 表面、播放原片并定格末帧；水晶死亡使用完整原版 EoG 死亡事件，按本次“原动画配套音频”要求不再沿用单位死亡 1.5 秒截断。出生/待机当场停止，爆炸播放到第 5 秒开始胜负播报，播报结束后用 1 秒淡出本局全部剩余声音。
- 原表列出的死亡漩涡/爆炸粒子未作为本次动画重建导入，未冒称完整 LoL VFX；权威生命、碰撞、比赛结束时机不改。

验证：完整 mechanics；实际渲染和录音 tools/demos/nexus_audio_lifecycle_review.gd，输出 /tmp/clash-nexus-audio。


2026-09-13 水晶内框遮挡修复（修正黑色补面）：保留原模型完整深井与中央水晶柱，水晶裁切下限设为 −10（低于源模型最低点约 −4.37），不再在 −1 高度截断内壁。正交相机射线投到地面后，限制地下片元只从半径 1.35 的开口显示，避免地下柱体伸出石框。移除上一版 `NexusCavityFloor` 黑色补面；此前“原模型无封底”的判断不成立，实际显示证实原生几何与纹理足以覆盖深井。蓝方恢复蓝色井壁，红方使用自身原生红色纹理。Main 不再按固定屏幕坐标给背景挖孔。公主塔仍按地面裁切，权威塔位、碰撞、音频与动画时长不变。

验证：实际主场景双阵营出生、待机及红方死亡/废墟截图已检查，石框内部显示原生深井，无黑色补面或草坪透出；完整 mechanics 通过。截图入口为 `tools/demos/nexus_audio_lifecycle_review.gd`。
