> 整理前快照，归档于 2026-09-13；含已被后续段落替代的数值与判断。当前入口见 [卡牌手册](../../../units/twisted_fate.md)。

# 卡牌大师（`twisted_fate`）

当前定义：[`scripts/data/cards/twisted_fate.gd`](../../../../scripts/data/cards/twisted_fate.gd)，使用 gameplay / visual / card_art / audio 四域；通用接入与验收见 [卡牌设计](../../../CARD_DESIGN.md)。

## 1. 属性数据

| 属性 | 数值 |
| --- | --- |
| 类型 / 费用 / 可选 | 单位 / 4 / 是 |
| 描述 | 远程法师，可攻击地面与空中目标；能在河道外的全图地面落点部署。 |
| 生命值 / 普攻伤害 | 460 / 62 |
| 攻击距离 | 190 |
| 移速 | 60 px/s（中） |
| 攻击间隔 / 首次命中 | 1.20 s / 0.25 s |
| 体型 / 权威半径 / 表现半径 | 中 / 18 / 22.5 |
| 质量 / 视野 | 3 / 250 |
| 空中单位 / 仅攻击建筑 / 可攻击空中 | 否 / 否 / 是 |
| 部署时间 | 预部署 1.0 s + 出现后部署 0.5 s |

费用和伤害是本次接入采用的初始平衡值，后续只需修改 `scripts/data/card_db.gd`。

## 2. 特殊部署

- `deploy_anywhere = true`：卡牌大师可在敌我双方任意地面区域落牌。
- 河道两行、桥面以及防御塔/水晶占用的格子仍不可部署。
- 通用 0.5 秒 Command Buffer 完成后，先进入 1.75 秒预部署：卡牌大师不生成，只在目标位置显示一圈卡牌提示。
- 预部署结束后由权威模拟直接生成可参战单位，`deploy_time = 0`，不显示单位部署读条，不再等待部署动画；首击仍遵循正常攻击前摇。

## 3. 被动

### 五次攻击

- 普攻表现按 `Attack1` → `Attack2` → `Attack3` → `Attack4` → `Spell3` 循环。
- 第五次攻击只发射一个弹体，在命中时一次结算基础伤害与额外 50% 伤害（62 + 31 = 93）。
- 被动通过通用 `attack_damage_multipliers = [1, 1, 1, 1, 1.5]` 配置，动画不参与伤害判定。

## 4. 主动技能：万能牌

- `kind = frontal`、`shape = fan`：朝锁定方向扇出 3 张牌，长度 `190`、角度 `54°`，命中敌方地面或空中单位造成 `100` 点伤害。
- 消耗 `1` 金币；每个实例最多使用 `2` 次；冷却 `8.0 s`。
- `Spell1` 的纯表现卡牌在施法 `0.25 s` 的出手 tick 才生成，随后用 `0.7177415 s` 飞向扇形末端；技能总时长仍为 `0.9677415 s`。Gameplay Impact 仍由固定 20Hz 权威模拟在同一出手 tick 结算，每个目标只结算一次，3 张牌不参与伤害判定。
- 施法锁定移动、攻击和朝向，表现动作使用 `Spell1`。

普攻也遵循同一时序：`first_hit = 0.25 s` 是 `Attack1/2/3/4/Spell3` 的出手/离弦点；攻击间隔仍为 `1.20 s`。固定 Tick 在这里生成权威弹体，弹体抵达目标后才结算伤害，第五击的额外伤害随同一个弹体结算。

## 5. 美术素材

- 正式包装场景：`res://assets/units/twisted_fate/twisted_fate_view.tscn`。
- 源素材已从 `待开发卡牌美术素材/卡牌大师.glb` 移动到 `assets/units/twisted_fate/source/twisted_fate.glb`，因此待开发队列少一个素材。
- 使用动画：部署 `twistedfate_2012_idle_enter_anm`，待机 `twistedfate_2012_idle1_anm`，移动 `Run1`，普攻 `Attack1/2/3/4` 与 `Spell3`，万能牌 `Spell1`，死亡 `Death`。
- 卡面：`res://assets/cards/twisted_fate_loading.png`，从 CommunityDragon 的基础皮肤加载图获取：<https://raw.communitydragon.org/latest/game/assets/characters/twistedfate/skins/base/twistedfateloadscreen.png>。

## 音频接入（2026-09-11）

已接入的动作、原始事件与文件见 [音频映射](../../../../assets/audio/units/twisted_fate/README.md)。完整动作对照、来源、自动验证及待人工试听项见 [全卡音频对照](../../../AUDIO_CARD_MAP.md)。本轮不改变既有 2D / 3D / 卡面状态；音频来自外部 LoL 原始库只读提取，未移动待开发队列素材。

## 原始动画表核对（2026-09-12）

本轮核对后保留当前动作配置。原图 Idle1 是 idle_enter→RAW_Idle1，映射到项目现有部署/待机；Spell3 与 Attack1 引用同一个原始动作资源，所以第五击换 Spell3 是玩法/特效语义，不能仅凭名称当成另一套骨骼动作。原图没有当前四套普攻、Spell3、Spell1 到 Run1/RAW_Idle1 的明确专用转场或非默认时长依据，不据此推断硬切或照抄 Death/转身层的零混合。万能牌 0.9677415 秒表现、0.25 秒出手、部署随后调整为 0.5 秒，0.8 秒死亡保留。原图的 Walk/Run1 速度阈值采用 LoL 单位，不直接套到本项目 px/s。来源与检查清单见 [远程组审查](../research/animation_group_ranged.md)。

## 首击停顿修复（2026-09-12）

通用 Unit 移除了旧移动装填规则：首次攻击无需先等待 1.2−0.25 = 0.95 秒。第二段真实部署结束同 Tick 可以开始正常 0.25 秒前摇或行军。再次接敌仅考虑上一击实际剩余冷却，不因只移动短距离而补等完整周期；攻击间隔、两段部署时长和音频素材不变。

## 部署调整（2026-09-13）

预部署保持 1 秒，落地后的部署锁定和部署动画缩放到 0.5 秒。`pre_deploy:start` 播放原版 `Play_sfx_TwistedFate_Gate_marker` 三个变体之一，各取原音频最后 1 秒，切入处仅做 5 ms 防爆音淡入，保留源增益及自然结尾。通过已有可靠卡牌事件 ID 同步与去重，不依赖单位已生成；声音不驱动部署计时。

验证：完整 mechanics 回归通过；新增预部署声音只触发一次/重复 ID 去重检查，更新 0.5 秒部署解锁首击及移动回归，并修复倒计时浮点余数多等一个 Tick 的情况。实际渲染检查双方预部署、落地和行军；host/join 均生成单位且各收到一次预部署声音。已录制实际主混音 `/tmp/tf_deploy_live.wav`，尾段素材电平较低，主观响度与听感仍待人工试听。

## 被动单弹体修正（2026-09-13）

第五击移除延迟 0.12 秒的追加攻击，改为同一个弹体携带 1.5 倍伤害。复用已接入的原版 E CardmasterStack 出手和命中素材。连续十击回归覆盖每次仅一个弹体、飞行前不扣血、第五/第十击各结算 93，其余 62；实际渲染检查双方各一发被动弹体，录制 `/tmp/tf-passive-live.wav`，主观试听仍待人工确认。

2026-09-13：完成[普攻发射素材核对](../research/projectile_launch_audio_audit.md)，资源归属与处理见报告。

2026-09-13：[Q 动画与发射音核对](../research/q_audio_animation_audit.md)。Q 补接 SealFateMissile_OnMissileLaunch 原始发射事件。

2026-09-13 后续：Q 已改为三张权威穿透牌，0.25 秒释放，碰撞时伤害与发声；同次施法每个目标最多一次伤害。

### 范围提示修正（2026-09-13）

用户确认原有模型、动画与音频测试通过。本轮仅修正万能牌范围提示：整片扇区改为三条真实穿透轨迹，与实际弹体共用方向函数；夹角按现有 `54° × 0.92`、每条从单位自身半径开始延伸 190px，路径宽 6px。地面提示标示权威路径，飞行卡牌仍按原有高度显示。双阵营实际渲染已核对；回归逐条比较权威路径与提示起点/方向/长度，并覆盖客户端范围 RPC 参数，host/join 冒烟正常。伤害、飞行速度与命中规则不变。

## 2026-09-13 完整落地音与即刻参战（当前）

预部署改为 1.75 秒（35 个权威 Tick），结束后直接出现在落点并加入战斗，不再经历 0.5 秒单位部署。工作台 immediate 只跳过命令缓冲，同样遵循卡牌预部署与声音。

pre_deploy:start 改用完整 Gate_marker 三个变体，原长约 3.75–3.85 秒，保留完整开头与尾部，经两段 atempo 保持音高压缩到精确 1.75 秒；不再截取最后一秒。音频播放仍通过已有可靠事件 ID 去重，出现时不重播。导入脚本 tools/audio/import_twisted_fate_deploy_audio.py，源文件只读，处理与哈希见音频 manifest。

mechanics 全部通过：1.70 秒仍无单位，1.75 秒生成且移动/攻击权限开放，音频各变体长 1.75 秒，重复事件不重播；零部署单位首 Tick 可寻敌开始正常前摇或移动。实战双阵营录音和 host/join 复核入口 tools/demos/twisted_fate_deploy_review.gd，输出 /tmp/clash-tf-deploy-175。

## 2026-09-13 事件音频补充

Spell4 已加入工作台音频页的“仅试听”事件：R1 Destiny 施放/生效/结束、R2 Gate 引导/完整落点，共 5 组。目录 `assets/audio/auditions/twisted_fate/`；工作台目录独立于运行时 audio 定义。当前 1.75 秒预部署和压缩落地声音不变，等待试听选择。

音频映射与剩余项见 [事件接入记录](../research/event_audio_expansion.md)。资源导入及完整 mechanics 回归通过，实际运行已录音；最终响度与尾音听感待人工复听。

## 2026-09-13 数值体系统一

当前权威施法窗口0.97秒、弹体飞行0.72秒。 当前规则以 [数值体系](../../../NUMERIC_SYSTEM.md) 及逐卡定义为准，前述日期记录保留历史值。

## 2026-09-13 试听后调整

用户试听后选定 R2 Gate_marker 原声前 1.75 秒，不变速、不压缩。三个 first_1_75s WAV 与原声前段逐采样一致。预部署 1.3 秒（26 Tick），出现后实际部署 0.45 秒（9 Tick），总计 35 Tick；预部署完成放在单位 Tick 之后，避免出生当帧重复消耗实际部署时间。声音仍从预部署起点播放一次。
