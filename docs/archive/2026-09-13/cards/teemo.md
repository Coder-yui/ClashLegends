> 整理前快照，归档于 2026-09-13；含已被后续段落替代的数值与判断。当前入口见 [卡牌手册](../../../units/teemo.md)。

# 提莫（`teemo`）

当前定义：[`scripts/data/cards/teemo.gd`](../../../../scripts/data/cards/teemo.gd)，使用 gameplay / visual / card_art / audio 四域；通用接入与验收见 [卡牌设计](../../../CARD_DESIGN.md)。

## 1. 属性数据

| 属性 | 数值 |
| --- | --- |
| 类型 / 费用 / 可选 | 单位 / 2 / 是 |
| 描述 | 灵活的远程射手，移速较快，擅长用毒针干扰敌人。 |
| 生命值 / 普攻伤害 | 230 / 38 |
| 攻击距离 | 160 |
| 移速 | 76 px/s（快） |
| 攻击间隔 / 首次命中 | 1.00 s / 0.25 s |
| 体型 / 权威半径 / 表现半径 | 小 / 12 / 16.5 |
| 质量 / 视野 | 2 / 210 |
| 空中单位 / 仅攻击建筑 / 可攻击空中 | 否 / 否 / 是 |
| 部署时间 | 1.0 s（默认值） |
| 弹体 | 380 px/s，`needle`；表现高度 42（`28 × 1.5`） |
| 溅射 / 击退 | 0 / 0 |
| 阵营颜色 | `Color(0.60, 0.80, 0.30)` |

## 2. 被动

无独立被动字段。当前数据中的干扰效果由主动技能“致盲”提供，普通毒针攻击本身没有额外持续伤害。

## 3. 主动技能

**致盲**（`kind = empowered_attack`）

- 当前主动技能数值：消耗 `0` 金币；每个提莫最多使用 `2` 次；每次使用后冷却 `4.0 s`。
- 强化下一次普通攻击，伤害倍率为 `1.0`，伤害数值保持基础值。
- 命中敌方单位后写入 `2` 层致盲；目标接下来的两次普通攻击仍会推进攻击动作和冷却，但不造成伤害，也不产生命中特效。
- 不改变攻击间隔；没有施法窗口和延迟，效果立即挂到下一次普攻上。

## 4. 使用的动画

- 场景：`res://assets/units/teemo/teemo_view.tscn`；`visual_forward_yaw = 0`。
- 部署 / 待机 / 移动：`Respawn` / `Idle1_Base` / `Run_Base`。
- 移动进入：`Run_In_ASU_Teemo_anm`。
- 普攻出手 / 命中后收势：`Attack1_ASU_Teemo_anm` / `Attack1_ToIdle`；Start 仍适配权威 0.25 秒出手，ToIdle 以 1.667 秒原片速度开始，允许下次攻击或移动提前打断。`attack_reference_interval = 1.0` 保持基础攻速调整和局内 Buff 的速率适配。
- 强化普攻出手 / 收势：`Spell1` / `Spell1_ToIdle`；强化攻击转移动画：`Spell1_ToRun`。
- 死亡：`Death`，表现时长 `0.8 s`。

## 音频接入（2026-09-11）

已接入的动作、原始事件与文件见 [音频映射](../../../../assets/audio/units/teemo/README.md)。完整动作对照、来源、自动验证及待人工试听项见 [全卡音频对照](../../../AUDIO_CARD_MAP.md)。本轮不改变既有 2D / 3D / 卡面状态；音频来自外部 LoL 原始库只读提取，未移动待开发队列素材。

## 原始动画表核对（2026-09-12）

- 普攻 Start→ToIdle 和连续普攻入口采用原表对应边的 0.05 秒混合；当前可达的强化攻击 Spell1 入口按原表使用 0.05 秒。未明确的边继续沿用通用混合。
- 按实际来源片段补齐 Idle 出口：普攻 Start→Attack1_ToIdle，普攻 ToIdle / Run / Run_In / Spell1_ToRun→Idle_In，Spell1→Spell1_ToIdle，Respawn→Spawn_toIdle。任何后续攻击或移动均可打断这些表现收势。
- 部署继续在 1 秒内播放整段 Respawn，新增的 Spawn_toIdle 只在部署结束且确实待机时播放，不延长部署锁定；死亡继续为 0.8 秒，未重新裁片。
- 普攻 Attack1/Attack2 的条件分支实际引用同一 attack1.anm 和同一 attack1_toidle.anm，所以继续使用一个可重复的普通攻击组合。没有为了节点名制造重复拳段或改变随机/伤害规则。
- 原图 Run_In 使用转角参数，Spell1_ToIdle/ToRun 共享同步组；本轮保留已有直向 Run_In 和强化转跑，不猜测原引擎的转角或相位算法。
- 新加的 Idle_In/Spawn_toIdle 没有原表音频事件；普攻弹体仍在 0.25 秒产生，强化起手及实际命中音仍由权威事件触发，音频素材/事件映射未改。

完整来源、保留项与待验清单见 [远程组审查](../research/animation_group_ranged.md)。

实际双阵营连续帧确认新增普通收势与部署收势可用。已有强化 `Spell1_ToIdle→Spell1_ToRun` 仍会重启翻身相位，直接回 Idle 也会快速混合落地；原图同步组尚未复现，按“不确定先不改”保留为已知局限。

2026-09-13 全面音频复核：[逐项结果与验证](../research/hero_audio_completeness_audit.md)。当前可明确对应的普攻/被动/已接技能缺项已补入；不引入原版未实现技能或未经确认的条件音。

2026-09-13：用户反馈音画正常；再次检查双方动画渲染、音频事件与资源接入，未发现需要修改的问题，保留现有模型、动画及音频。
