> 整理前快照，归档于 2026-09-13；含已被后续段落替代的数值与判断。当前入口见 [卡牌手册](../../../units/sun_disc.md)。

# 太阳圆盘（`sun_disc`）

当前定义：[`scripts/data/cards/sun_disc.gd`](../../../../scripts/data/cards/sun_disc.gd)，使用 gameplay / visual / card_art / audio 四域；通用接入与验收见 [卡牌设计](../../../CARD_DESIGN.md)。

## 1. 属性数据

| 属性 | 数值 |
| --- | --- |
| 类型 / 费用 / 可选 | 建筑 / 4 / 是 |
| 定位 | 对空对陆远程建筑；在防御塔废墟上获得永久基座收益 |
| 生命值 / 普攻伤害 / 攻击距离 | 1200 / 105 / 220 |
| 攻击间隔 / 首次出手 | 1.40 s / 0.50 s |
| 弹体速度 | 440 px/s |
| 权威半径 / 表现半径 | 40 / 60 |
| 占地 | `3 × 3` 格（`Vector2i(3, 3)`） |
| 空中单位 / 仅攻击建筑 / 可攻击空中 | 否 / 否 / 是 |
| 普通建筑寿命 | 40.0 s；生命匀速衰减为 0 |
| 塔墟建筑寿命 | 不倒计时、不自然衰减，直到受到足量伤害 |

费用、对空对陆和占地来自需求；未指定的生命、伤害、射程、攻速、弹体与护盾数值采用本次初始平衡值，后续只需修改 `scripts/data/card_db.gd`。

## 2. 防御塔废墟基座

- 存活防御塔继续由原有占地规则封锁，不能与太阳圆盘重叠。
- 公主塔摧毁后，其权威 `3 × 3` 占地仅对太阳圆盘继续保留：圆盘自身占地只要擦到塔墟就非法。
- 唯一例外是圆盘中心格位于该塔墟九格中的任意一格；九个中心点全部视为“建于塔墟”，包括普通 pocket 区外的敌方已毁塔位。
- 主机在生成时固化 `built_on_tower_ruin` 并通过可靠生成 RPC 同步。该状态令本次建筑的 `lifespan = 0`、`lifespan_hp_decay = false`；客户端不能自行推导或改变权威寿命。
- 已毁塔的圆形碰撞与导航障碍仍按原机制解除；太阳圆盘只注册自身 `radius = 40` 的新圆柱碰撞与动态导航障碍，不恢复旧塔碰撞。
- 普通地面部署显示圆盘包装自带的静态防御塔废墟；建于真实塔墟时隐藏自带废墟，直接复用 `TowerModel3D` 已保留的 Rubble，避免双层模型。

## 3. 普通攻击与主动技能

- 普攻可选择地面或空中目标，使用 `Attack1_BASE`；权威出手生成金色追踪光球，纯表现起点固定在圆盘中心（地面点上方 `154px`，无前向偏移），动画不驱动命中。
- 主动“日耀庇护”使用通用 `area_shield`：消耗 `1` 金币、每个建筑实例最多 `1` 次、无重复使用冷却需求。
- Gameplay Impact 立即按普通攻击相同的表面距离口径，给攻击范围 `220` 内所有存活友方战斗对象增加 `180` 点护盾，持续 `6` 秒：包含地面/空中单位、友方建筑卡、自身、防御塔与水晶。
- 护盾优先承受外部伤害，但不是生命值。限时建筑的自然寿命衰减仍直接扣除 `hp`，生命衰减到 `0` 时即使护盾尚存也会死亡；塔与水晶护盾随固定快照同步给客户端。

## 4. 美术素材

- `待开发卡牌美术素材/沙漠皇帝 (3).glb` 已移动到 `assets/units/sun_disc/source/sun_disc.glb`；待开发队列中的源文件已移除。
- 蓝红双方公主塔模型的最终 `Rubble` 表面通过 `tools/assets/export_princess_tower_ruin.py` 烘焙为两个静态、单表面 GLB；运行时没有部署或死亡动画。普通地面部署时自带废墟直接出现，并在圆盘 `Death` 播放完的末帧随组合包装移除；建于真实塔墟时不重复生成或移除废墟。
- 正式包装场景为 `sun_disc_blue_view.tscn` / `sun_disc_red_view.tscn`，仅废墟颜色按阵营区分。
- 圆盘按索敌方向转动，自带废墟锁定部署首帧的世界朝向。`Spawn` 使用世界地面高度裁切并保持正常深度遮挡：地下碎片不渲染，位于废墟内部的部分由废墟遮挡，升出废墟后才逐渐显露并完成拼合。
- 动画映射：部署 `Spawn`、普攻 `Attack1_BASE`、死亡 `Death`；待机使用通用 `idle_cycle` 按 `Idle1_Base → Idle1_Base → Idle2_Base` 循环。
- 卡面 `assets/cards/sun_disc_loading.png` 由 `tools/capture/capture_sun_disc_card_art.gd` 使用正式组合模型拍摄。

## 音频接入（2026-09-11）

本轮保持静音，缺口与原因已记录。完整动作对照、来源、自动验证及待人工试听项见 [全卡音频对照](../../../AUDIO_CARD_MAP.md)。本轮不改变既有 2D / 3D / 卡面状态；音频来自外部 LoL 原始库只读提取，未移动待开发队列素材。

## 2026-09-12 原始动画表复核

已核对AzirSunDisc原图；原版含Disk骨骼遮罩与随机选择，当前基本片段映射保留，未将随机规则误作固定动作链。

来源、行号与保留理由见 [兵线与建筑对照](../research/animation_group_buildings_minions.md)。

## 2026-09-13 事件音频补充

日耀庇护已接入装备 3190 钢铁烈阳之匣：`shield:cast` → `Play_sfx_3190Active_OnCast`，每次施放一次；`shield:applied` → `Play_sfx_3190Shield_OnBuffActivate`，每个实际获盾的友军/建筑/塔/水晶位置一次。使用可靠卡牌事件 ID 去重，护盾数值与范围不变。圆盘自身普通攻击/生成/死亡仍未接入。

音频映射与剩余项见 [事件接入记录](../research/event_audio_expansion.md)。资源导入及完整 mechanics 回归通过，实际运行已录音；最终响度与尾音听感待人工复听。

## 2026-09-13 试听后调整

护盾生效声继续使用获盾目标坐标（不是施法者坐标），在源事件增益上额外降低 24 dB；施放声保持不变。小兵保持源增益，未提高音量。

## 最新试听决定

取消 shield:applied 护盾生效声，仅保留 shield:cast 钢铁烈阳之匣施放声。护盾结算与原始素材归档不变。

## 2026-09-13 太阳圆盘与墓碑正式接入

太阳圆盘已接入 AzirObeliskSound_OnBuffCast 生成、OnBuffDeactivate 消失、共享防御塔出手/发射/命中，原盾技能施放声保持，获盾声仍不使用。来源与处理见各音频目录 building_event_manifest.json；导入器 tools/audio/import_sun_disc_tombstone_audio.py。

- 当前保留 1 秒权威部署。Spawn 原片约 4.9667 秒适配这 1 秒；生成声音的前 4.9667 秒保音高压缩到 1 秒，其后的消散尾音保持原速，原声音内 3.5 秒的延迟层约在 0.705 秒进入。旧动画表的 Obelisk_buffcast/buffactivate 与当前声音银行 ObeliskSound 事件名称不同，采用语义/时序适配，不宣称恢复原客户端脚本。
- Death 恢复原片约 3.6667 秒；消失声完整播放，不套单位死亡 1.5 秒截断。权威死亡即时生效，动画和尾音不延迟伤害、碰撞或寿命结算。
- Attack1_BASE / Attack2_BASE 都启用，沿用项目确定交替选择；按原图 Disk 遮罩仅保留 Chest_Loc / Obelisk_Tip 骨骼轨道，不驱动基座。库在实例中复制，原 GLB 保持不变。
- Idle 参考原图 60/40 分支：三次简单 Base、两次 Base→Idle2→Base，展开成 9 段确定循环；没有新增原版随机选择器。相关片段间采用原表零混合。
- 普攻原始事件增益保留，项目三层分别额外 −6 dB，为双方同时攻击预留混音余量。出手读攻击序号，发射读真实弹体创建，命中读真实碰撞；音频不驱动弹道或伤害。
- 墓碑已接 YorickWWallLife_OnBuffActivate 持续层：部署结束进入待机开始，结束续播，冻结/眩晕暂停，死亡/销毁/清场停止。项目墓碑普通和瞬时召唤主动均可保持待机，不把生存循环当普攻。

验证：完整 mechanics 通过，新增墓碑持续声生命周期检查，更新太阳圆盘双攻击遮罩及待机循环回归。双阵营实际渲染、事件日志和混音录音来自 tools/demos/sun_disc_tombstone_review.gd，输出 /tmp/clash-sun-disc-tombstone。主观听感仍需用户复听。

### 本轮试听调整

- 死亡动画仅播放 Death 原片 0–2 秒，压缩为 0.8 秒（2.5 倍速）；完整消失音暂不裁剪，供用户试听选段。
- 日耀庇护增加黄色扩散波：中心向自身半径 40 + 技能/攻击射程 220 的边界扩散，约 0.28 秒到达、0.5 秒完全淡出。沿用去重的可靠 shield:cast 表现事件，主机/客户端均消费；音频静音不会阻止特效，波前不驱动加盾。清场时一起清理。
- 临时音频板合并 building_event_manifest，现可按 death 试听完整消失声的两个变体（约 4.97 / 5.09 秒），带播放器时间标识。
- 完整机制回归通过；实际双阵营画面见 /tmp/clash-sun-disc-tombstone/shield_wave.png，死亡片段截图与录音同目录。

最新死亡音选择：两个消失音变体均仅取原始前 2 秒，以 atempo=4/3 保音高压缩到精确 1.5 秒，不添加旧的 1–1.5 秒死亡淡出包络；试听板与实战使用同一文件。死亡动画仍为前 2 秒压缩到 0.8 秒。
