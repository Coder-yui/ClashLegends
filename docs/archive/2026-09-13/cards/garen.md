> 整理前快照，归档于 2026-09-13；含已被后续段落替代的数值与判断。当前入口见 [卡牌手册](../../../units/garen.md)。

# 盖伦（`garen`）

当前定义：[`scripts/data/cards/garen.gd`](../../../../scripts/data/cards/garen.gd)，使用 gameplay / visual / card_art / audio 四域；通用接入与验收见 [卡牌设计](../../../CARD_DESIGN.md)。

## 1. 属性数据

| 属性 | 数值 |
| --- | --- |
| 类型 / 费用 / 可选 | 单位 / 5 / 是 |
| 描述 | 高生命值的近战战士，专注攻击建筑，适合在前线持续推进。 |
| 生命值 / 普攻伤害 | 1050 / 88 |
| 攻击距离 | 28 |
| 移速 | 44 px/s（慢） |
| 攻击间隔 / 首次命中 | 1.10 s / 0.38 s |
| 体型 / 权威半径 / 表现半径 | 大 / 24 / 28.5 |
| 质量 / 视野 | 8 / 220 |
| 空中单位 / 仅攻击建筑 / 可攻击空中 | 否 / 是 / 否 |
| 部署时间 | 1.0 s（默认值） |
| 弹体 / 溅射 / 击退 | 无 / 0 / 0 |
| 阵营颜色 | `Color(0.35, 0.55, 0.90)` |

## 2. 被动

无独立被动字段。盖伦的核心定位由高生命值和 `building_only = true` 决定：普通攻击只寻找塔和建筑，不攻击普通单位。

## 3. 主动技能

盖伦有两个主动技能候选，备战时选择其中一个带入战斗；同一个盖伦实例仍只携带一个主动技能槽。

**致命打击**（`kind = empowered_attack`）

- 当前主动技能数值：消耗 `1` 金币；每个盖伦最多使用 `2` 次；每次使用后冷却 `6.0 s`。
- 强化下一次普通攻击，伤害倍率为 `2.0`，本次命中后立即消耗强化。
- 强化尚未打出且单位不在当前攻击动作中时，移速倍率为 `60 / 44 = 1.363636...`，即从 44 提升到 60 px/s（中）。攻击动作进行中才使用技能时不追加移动加速。
- 不重置、不延后当前攻击的 `attack_cd` 或 `first_hit`；技能没有施法窗口和延迟，Gameplay Impact 立即写入“下一次普攻强化”状态。

**审判**（`kind = continuous_area`）

- 消耗 `2` 金币；每个盖伦最多使用 `1` 次；每次使用后冷却 `10.0 s`。
- 持续 `3.0 s`，每 `1.0 s` 以盖伦当时的权威位置为中心，对半径 `90` 内的地面敌人造成 `60` 点伤害，共 3 次脉冲；范围会跟随盖伦移动，不使用固定落点。
- 施放期间只锁定 `attack`，盖伦仍可移动和改变朝向；技能持续时间与伤害脉冲由固定模拟计时，不由动画完成事件驱动。

## 4. 使用的动画

- 场景：`res://assets/units/garen/garen_view.tscn`；`visual_forward_yaw = 0`。
- 部署：`Respawn_Base`。
- 待机 / 移动：`Idle1_Base` / `Run_Base`。
- 强化待机移动：`Run_Spell1`。
- 普攻：`Attack1`、`Attack2`。
- 强化普攻：`Spell1`（仍走 Attack 通道）。
- 审判：`Spell3_0`，通过 `visual_actions.judgment` 播放完整 `3.0 s` 技能动作（素材实际动画名使用下划线）。
- 死亡：`Death`，表现时长 `0.8 s`。

### 原始动画表校正（2026-09-12）

已核对基础皮肤 `AnimationGraphData` 的节点和 `.anm` 资源路径。`Spell3_0` 的当前入口、退出边均为零混合；审判仍完整播放 3 秒，移动与每秒伤害逻辑保持。Q 从部署、待机、移动或普攻进入时采用原表 0.1 秒混合，审判→Q 保留原表的零混合例外；`Attack2→Attack1` 也恢复为零混合。

两段普攻均是原表完整 AtomicClip，没有证据将其另拆为 Start/Hit。原始审判还有朝目标方向的参数节点，项目继续选 `Spell3_0`，不增加原版转向轨道。部署 1 秒、死亡 0.8 秒和现有选片不改；原始图并不能确定适合本项目的死亡裁剪点。证据及未采纳项见 [近战动画对照](../research/animation_group_melee.md)。

## 5. 音频

旧实验普攻音频已删除，替换为本次原皮事件解析库中实际使用的 42 份 WAV。
覆盖两段普攻出手/命中、致命打击准备/强化出手、审判开始/持续旋转/结束/真实命中以及死亡。审判空转也有声音；持续片段跟随单位，控制暂停/恢复，结束或死亡停止，不无限循环。
仍然只使用备战选中的一个技能；未接 W、R、被动、表情或台词。
配置在 CardDB.audio；详细事件映射、来源与解析限制见 `assets/audio/units/garen/README.md`。

## 音频接入（2026-09-11）

保留当前已接音频，见 [音频映射](../../../../assets/audio/units/garen/README.md)。完整动作对照、来源、自动验证及待人工试听项见 [全卡音频对照](../../../AUDIO_CARD_MAP.md)。本轮不改变既有 2D / 3D / 卡面状态；音频来自外部 LoL 原始库只读提取，未移动待开发队列素材。

2026-09-13 全面音频复核：[逐项结果与验证](../research/hero_audio_completeness_audit.md)。当前可明确对应的普攻/被动/已接技能缺项已补入；不引入原版未实现技能或未经确认的条件音。

## 2026-09-13 数值体系统一

强化移速倍率整理为1.3636（136.36%），经两位有效移速计算仍为60。 当前规则以 [数值体系](../../../NUMERIC_SYSTEM.md) 及逐卡定义为准，前述日期记录保留历史值。
