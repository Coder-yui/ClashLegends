# 项目整理与验证（2026-09-13）

本次以任务开始时已暂存、未暂存及未跟踪的内容为基线。原 Git index 保持逐字节一致，没有提交、重置或覆盖已有升级。正式玩法数值、20Hz 时序、Snapshot/RPC、卡牌音频池和模型内容保持原状。

## 整理结果

| 范围 | 处理 |
| --- | --- |
| 运行代码 | 抽出 CardDetails 纯展示数据，DeckBuilder 保留控件、卡组与选择状态；空间音频播放器共用配置工厂，原有池、事件、停止和混音生命周期不变 |
| 权威与表现边界 | 核对 Main、Unit、battle、presentation 的现有分层；保留固定时钟、统一生成/施法/出牌与只读表现，不因行数继续拆出万能控制器 |
| 测试 | mechanics_check 直接编排套件，删除空继承入口；工作台契约独立成 workbench_suite，保持在主动技能回归中的原调用顺序；移除 tests 下重复横扫演示场景 |
| 退出清理 | 场景释放后等待 0.25 秒，让 headless 快速回归的音频线程释放流；基线偶发 ObjectDB/资源退出错误在最终运行未再出现，不声称所有设备和所有退出路径均已认证 |
| 当前文档 | 24 张逐卡手册统一为当前机制、资源/迁移、验收/缺项；音频素材 README 汇总当前入口并保留 manifest 溯源；同步工作台、音频、维护架构、测试与开发状态 |
| 历史 | 研究报告与源表、逐卡完整修订、音频旧说明、数值基准移入本日期归档；修复引用，保留撤回原因与临时证据的适用时间 |
| 工具 | 音频 Python 与白名单集中 tools/audio，模型加工集中 tools/assets；通用导入器 --cards 必填，支持 --plan 与 --dry-run；维护审计提供链接/资源/注册/UID/语法检查和只读素材清单 |
| 素材 | 退役第一版男爵特效、旧地图图集/提示与背景实验移入 assets/archive 并忽略导入；仍复用的 overlay shader 移到当前 baron_minion 目录，内容未改 |
| 中间产物 | 删除 Python 缓存、素材目录 Finder 缓存与已归档素材的 import sidecar；忽略 Python 缓存，docs 和 builds 使用 .gdignore；现有可再生截图/录音仍留在忽略目录 |
| 新卡流程 | 新增接入清单与卡牌文档模板；明确五阶段、队列移动、模型依赖、音频选择范围、字段读取/校验与联合验收 |

DeckBuilder 从 1115 行收敛到 869 行；CardDetails 250 行，供 UI 和既有内容断言共用。工作台回归从主动套件独立出 192 行。所有 24 个定义、双方/形态的展示属性、被动和技能文本与整理前输出完全一致。

## 本次验证

环境：本机 Godot 4.7.1 标准版，Compatibility / Apple M5。

| 验证 | 结果与证据 |
| --- | --- |
| 整理前机制基线 | 706 项通过；退出有资源清理警告/错误 |
| 最终完整机制 | 706 项通过，最终日志无 SCRIPT ERROR、ERROR 或退出资源未释放提示；`/tmp/clash-cleanup-final-mechanics.log` |
| 导入与脚本解析 | Godot editor import 完成；音频脚本最终 check-only 通过 |
| 仓库审计 | 24 个定义与文档对应；本地链接、字面资源、UID、Python 语法检查 0 错误；`/tmp/clash-cleanup-final-inventory.json` |
| Python 审计边界 | 3 个临时夹具测试通过，涵盖正常链接、模板排除、真正缺失、孤立 UID、注册/文档与外部素材边界 |
| 音频导入预览 | 明确选择 anivia/gnar_mega 的 dry-run 通过，不读取/写入声音资源 |
| 文本等价 | 24 个定义和变形的卡牌展示输出与任务基线逐项相等 |
| 本机 host/join | 并行运行约 18 秒，客户端收到至少 280 份快照，覆盖单位生成、纳尔大小形态、定向技能、弹体、独立命中来源及持续吐息；日志无脚本/资源错误，随后受控结束进程 |
| 卡牌详情渲染 | numeric_review：查看艾希、皮克斯的属性、主动说明与卡面，输出 `/tmp/clash-numeric-review/` |
| 工作台渲染 | workbench_preview：模型、实战、音频、记录、法术与形态页面；切页暂停、返回继续和清空检查通过；查看实际模型和音频页 |
| 战斗渲染 | maintenance_preview：攻击、控制/变形、死亡、恢复四帧；查看双方战场表现，输出 `/tmp/clash-maintenance-render/` |
| 材质迁移渲染 | baron_minion_preview：八个单位、八个阶段；查看持续、受击/控制、到期恢复，输出 `/tmp/clash-baron-minions/`，无 Shader/脚本错误 |
| 差异检查 | git diff --check 通过；原暂存补丁与基线一致 |

临时日志和图像是本机证据，可能被系统清理。复现命令见 [测试手册](../../../tests/README.md)，不能以临时文件未来缺失推断测试未执行。

## 保留理由与边界

- 现有 Main/Unit 仍承担其权威编排和通用实例职责；本次没有改移动、索敌、伤害、比赛规则或联网载荷。后续性能拆分应以代表性对局测量为依据。
- 保留 GLB 同组外部纹理、Blender 源件、候选地图与 source_props；哈希相同不代表可以破坏模型相对路径。
- 保留按原事件命名的同内容 WAV、来源清单和工作台试听文件，避免改变随机池权重或丢失事件身份。旧音频配方未全量重新解码；外部源库、后续专项加工及当前 audio 域必须一起核对。
- 两个项目外素材目录及其内容未迁移、未清空；没有新卡立项时不处理用户的待开发素材队列或宣传素材。
- 没有逐卡重新进行全量主观试听，也没有手机、广域网和高丢包长测。已有缺项继续在 [开发状态](../../DEV_PLAN.md) 与 [音频表](../../AUDIO_CARD_MAP.md) 维护。

当前操作入口：[新卡与素材清单](../../NEW_CARD_CHECKLIST.md)、[维护架构](../../MAINTENANCE_ARCHITECTURE.md)、[工具目录](../../../tools/README.md)。
