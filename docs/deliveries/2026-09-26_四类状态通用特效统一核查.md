# 交付合同：四类状态通用特效统一核查

- **需求 / 来源**：用户对话；目前所有回血、减速、眩晕、减攻速统一使用项目通用特效。
- **完成结果**：检查逐卡定义、战斗结算、表现脚本、模型包装与工作台入口，确认现有真实回血统一触发 `restoration_heal_effect.gd`；有效移动减速和减攻速统一使用 `soft_control_effect.gd`；单位、建筑卡与防御塔的眩晕统一使用 `stun_effect.gd`，未发现需要替换的卡牌私有旧状态绘制。本轮无需再改运行代码。在[表现合同](../status/PRESENTATION.md#统一接入约定)记录全部当前来源及唯一入口，并在[任务导航](../AGENT_WORKFLOW.md)明确后续新增卡牌同样必须复用。
- **边界**：治疗术范围、技能弹道、命中和区域显示继续表达施法/范围信息，受影响对象的状态提示仍统一使用通用特效。复生与生命上限调整不伪装成治疗；不改变目标资格或增加新玩法。当前减攻速只有工作台通用施加入口，正式卡牌尚未施加该状态，新来源自然复用现有表现。
- **验证**：基线 `2ecbf51`，保留工作区已有大量未提交修改。Godot `4.7.1.stable.official.a13da4feb`，本轮完整机制回归 **7107 项断言通过**，维护审计 0 错误，`git diff --check` 通过。日志在本地素材库 `04-中间产物/预览与验证/通用状态特效入口核查-2026-09-26/mechanics.log`。
- **查看方式**：卡牌开发工作台实战页；表现合同表格可查询每类效果的现有来源与公共入口。
- **遗留 / 未验证**：本轮只核查入口并固化约定，未更改美术参数、未重复逐卡目视验收；已有观感仍待用户确认。
