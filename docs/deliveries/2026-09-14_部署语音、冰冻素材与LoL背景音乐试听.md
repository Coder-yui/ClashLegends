# 交付合同：部署语音、冰冻素材与 LoL 背景音乐试听

- **需求 / 来源**：用户对话；更新英雄部署声方案、复查 `FirstLight_CR` 冰冻音频，并准备非播报的 LoL 游戏背景声试听。
- **完成结果**：
  - 艾尼维亚、艾希、小纳尔分别接入两段未压缩英雄短语音，使用 `deploy:voice` / `Voice` 总线随机二选一。
  - 剑圣接入 `Play_vo_MasterYi_Attack2DGeneral` 的原始一段渲染，未做一秒压缩。
  - 盖伦、格温、赏金猎人的部署候选全部限制为不超过 5 秒，仅放试听台，不改变正式卡牌配置；格温原始 `Respawn3D` 5.841 秒保留作对比。
  - 试听台更新为 24 项：已接入部署声参考、上述待定英雄短语音、LoL 丽桑卓冰冻替代候选，以及 Map22 的 K/DA、Lux、PROJECT、Paris Open 四组背景音乐。兵种、皮克斯、小鬼、凤凰蛋相关替代候选已移除。
  - `FirstLight_CR` 无 wav/ogg/mp3/flac/bnk/wem/wpk 等音频文件，未发现可直接复用的皇室战争冰冻素材。
  - 按用户决定记录暂缓：兵种新增死亡/男爵强化声；皮克斯部署/死亡/汲取相关声；小鬼普通命中声；凤凰蛋碎壳声。现有已接入声音保持不变，未来可重新处理。
- **验证**：
  - 7 段正式英雄语音已核对时长：0.848–1.831 秒；3 组待定英雄候选为 1.231–1.991 秒，均 ≤5 秒。
  - Map22 音乐银行从 `Map22.wad.client` 提取并经 `wwiser`/`vgmstream-cli` 解码：K/DA 16.000 秒、Lux 134.572 秒、PROJECT 124.235 秒、Paris Open 114.750 秒；均标记为非播报、仅试听。
  - 所有试听台清单路径存在；4 个正式事件清单 JSON 可解析。
  - `python3 tools/maintenance/audit_project.py`：`Audit: 24 cards, 2488 files, 0 errors`。
  - Godot 4.7.1 `tests/mechanics_check.gd`：3290 项断言全部通过；实际主场景启动成功。回归中保留已有水晶终局音频缺失 warning，不属于本次变更。
  - 用户尚未确认主观听感或最终选曲。
- **查看方式**：启动 `python3 tools/dev.py audio --manifest 'builds/deployment_audio_review/manifest.json' --port 18765` 后打开 `http://127.0.0.1:18765/`；本次任务的试听台由该清单驱动。正式英雄声音也可在各卡牌音频页试听。
- **遗留 / 未验证**：背景音乐尚未接入项目；冰冻仍未正式接入；盖伦、格温、赏金猎人候选等待用户听后决定。LoL 原始素材的授权边界不在本任务内确认。
