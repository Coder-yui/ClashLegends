# 交付合同：峡谷先锋定向修复

- **需求 / 来源**：按用户对话核实并修复峡谷先锋准备重启动画、外部眩晕映射、冲撞期间主动技能锁与相关音频生命周期。
- **完成结果**：
  - 准备取消现在会清理旧动作序列、覆盖恢复记录和续播资格；控制期间只保持当前姿势，不恢复已取消动作。新一轮同名准备按 `VisualActionSequence` 的片段起点对齐，晚到快照仍按权威已流逝进度对齐。
  - 先锋新增局部 `awaiting_reprepare` 标记：准备被硬控/击退打断后，在重新决策前继续锁定主动技能；目标失效、需要重新追击或寻找新起点时退出本轮并恢复普通 READY 权限。主机与客户端共用现有 action-permissions 快照，没有扩展协议，也没有新增冲撞状态机。
  - 移除先锋专属 `"stun_loop": "Stun"` 配置；`rush_hit -> Dash_Hit`、`RECOVERY` 管理的 0.65 秒恢复、伤害/生命代价/寻路/召唤及金币、次数、冷却、退款逻辑均未改动。
  - 已核实音频管理器原有按动作序号停止旧准备持续声、启动新序号持续声的实现正确，因此未修改音频系统；仅补充了取消、跳过取消帧和同名新序号测试。
  - 卡牌详情与先锋主动技能描述补充说明准备、冲撞及撞后收势期间不能施放其他主动技能。
- **验证**：基线提交 `8dd1da8`；工作区保留 8 个相关文件的未提交修改；实际引擎为 Godot 4.7.1.stable。针对性回归通过 523 项断言；全量 `tests/mechanics_check.gd` 通过 4554 项断言，含 `ActiveSkillSuite`、`AnimationStateSuite`、`AUDIO_PRESENTATION_SUITE_SCRIPT`、`network_lifecycle_suite` 和 `StructureRushSuite`；`git diff --check` 通过。运行 `tools/demos/structure_rush_review.gd` 完成双方实际渲染，截图和 `battle_mix.wav` 输出于 `ClashLegends-开发素材库/04-中间产物/预览与验证/1789812194-44921/structure-rush/`，已目视检查控制/准备/冲刺/撞击恢复画面及事件日志。
- **查看方式**：查看[先锋冲撞测试](../../tests/suites/cards/structure_rush_suite.gd)、[动画控制器](../../scripts/presentation/unit_model_3d.gd)和[本次渲染输出目录](<../../ClashLegends-开发素材库/04-中间产物/预览与验证/1789812194-44921/structure-rush/>)。
- **遗留 / 未验证**：当前环境不能替用户完成人工听辨，故未把 `battle_mix.wav` 写成“听感已确认”；自动化已确认准备音停止/重启及冲刺、沿途命中、撞击命中事件没有串触发。全量测试中的联机连接等待和既有 `world_nexus_chaos` 缺失资源 warning 与本次先锋改动无关。
