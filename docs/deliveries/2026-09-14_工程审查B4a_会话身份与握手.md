# 交付合同：工程审查 B4a — 会话身份与握手

- **需求 / 来源**：[工程审查 CL-04](../issues/2026-09-14_工程审查报告.md)：唯一对手、一次性卡组确认、双端就绪、来源与阶段门禁、断线退出。
- **完成结果**：新增 `MatchSession` 持有大厅/加载/运行/结束/断线阶段。主机绑定唯一对手后，双方校验协议、内容指纹及合法卡组；各自建立战场并确认就绪后才运行。局中卡组注册、重复开局、第三方连接均不能重置对局。部署/技能限定当前会话的绑定对手控制队伍 1，单调请求序号阻止重复执行；表现事件携带会话标识。加载超过 30 秒停止；连接失败保留菜单提示，局中断线统一清理并提供返回菜单。协议升为 15，双端须同时更新。
- **验证**：基线 `5eab9a30fd4f380464fd1c111534db62387c6afe`，保留既有未提交修改，引擎 `4.7.1.stable.official.a13da4feb`。改前复现“未绑定调用者注册且局中重注册重置手牌”，见 `builds/review-fixtures/cl04-baseline.log`。2253 项机制断言、18 项验证工具测试通过；真实双进程对局插入第三个 ENet 连接、重复出牌、旧会话技能及局中重注册后，终态仍一致且仅出牌一次。另有协议不一致、内容不一致、慢加载、中途断线、返回按钮重载后同端口第二局五组双进程用例全部通过。慢加载保持主机 Tick 0；不兼容版本在创建塔和手牌前拒绝；第二局会话、命令序号和结算界面重新初始化。
- **证据 / 查看方式**：运行 `python3 tools/verify.py --network-boundaries`。本轮完整证据见 [result.json](../../builds/verification/20260914-122014-279483/result.json)，各子目录保存主机与客户端日志。测试故障注入位于 `tests/fixtures/network_boundary_main.gd`，正式 Main 不包含故障场景开关。
- **遗留 / 未验证**：CL-05 建筑占位事务仍属 B4 后半段，尚未完成。未做 WAN/跨设备或人工鼠标点击验收；返回菜单测试触发实际按钮连接并执行场景重载，不能当作人工交互确认。当前目标为 Mac，手机不作为发布目标。规则算法或 RPC 协议变更须提升版本；卡牌定义变化自动改变内容指纹。
