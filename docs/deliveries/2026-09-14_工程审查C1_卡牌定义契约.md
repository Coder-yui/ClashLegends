# 交付合同：工程审查 C1 — 卡牌定义契约

- **需求 / 来源**：[工程审查 CL-08](../issues/2026-09-14_工程审查报告.md)。保留平铺运行格式和递归只读，在编译前建立可检查的域契约，避免错误类型让校验器自身崩溃。
- **完成结果**：CardDefinitionCompiler 检查原始四域、技能列表和变形配置的归属、数量与冲突；24 张卡将表现字段迁入 visual，形态声音迁入 audio 对应形态，规则字段保持 gameplay。CardShapeValidator 在语义/资源访问前检查类型与有限性；注册表坏键、坏值也返回错误。音频 events 整数反例不再调用不存在的 has 方法，异常诊断带字段路径、期望类型和实际值。
- **验证**：基线 `5eab9a30fd4f380464fd1c111534db62387c6afe`，保留已有未提交修改；Godot `4.7.1.stable.official.a13da4feb`。迁移前保存当前 24 张卡完整编译配置，迁移后逐值完全相等，见[配置对照](../../builds/review-fixtures/domain-compare.log)。2972 项机制断言、18 项工具测试、导入、审计及真实双进程联网通过，见[验证结果](../../builds/verification/20260914-130529-630944/result.json)。新增域/冲突/深复制测试，以及艾希、纳尔、格温每个配置叶子的错误类型测试；无意外脚本错误。模型、卡面、动画及音频仍经过既有内容/表现契约套件。
- **查看方式**：写法见[四域契约](../CARD_DESIGN.md#原始定义的四域契约)，代表实现看纳尔与盖伦定义。运行 `python3 tools/verify.py --timeout 60`。
- **遗留 / 未验证**：未改素材，不声称新增人工观感/听感验收。CL-06/07、D/E 板块仍未完成；类型阶段通过后仍需原有语义及资源校验，域正确不等于新卡内容已经完成验收。
