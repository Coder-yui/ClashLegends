# 交付合同：盖伦格温部署音频与当日整理

- **需求 / 来源**：用户对话；按[布锅锅语音站](https://voice.buguoguo.cn/#/voice)定位本地六段部署素材，整理当天改动并修正文档。用户已撤回模型遮挡事项，本轮未处理。
- **完成结果**：盖伦“人在塔在 / 勇往直前 / 保家卫国”，格温“英雄专属锁定音效 / 你要找裁缝吗？ / 剪刀飞快”，分别接入单个 `deploy:voice` 三选一池，等权、避免连续重复、Voice 总线、额外 0 dB。重绑与晚到不补播，不在移动/普攻额外触发这些台词。
- **代码与目录**：工作台正式音频配置展开移至 [WorkbenchAudioCatalog](../../scripts/ui/workbench/audio_catalog.gd)，UI 保留播放/筛选；去除不存在的 launch 音量字段读取。统一演示入口为 `tools/demos/demo_xin_sweep.tscn`，重复的 `tests/demo_xin_sweep.tscn` 与根目录 `wwnames-out.txt-20260914185039.txt` 移至 `builds/cleanup/2026-09-14/` 保留，未删除原始素材。
- **文档同步**：[当前状态](../DEV_PLAN.md)归整今日战斗、网络、治疗术、音频与工具变动；补充工作台正式/候选边界与 `active:cast` 事件说明；更新两卡音频页、来源清单和缺项表。[维护架构](../MAINTENANCE_ARCHITECTURE.md)及建筑到期交付中的错误说明已修正：协议与 Snapshot 同为 **21**，单位载荷仍为 40 项。历史归档保留原时点记录。
- **基线 / 环境**：`dc3a16bee611f0706c8f516c8f5be05985f1b4ef`；开始时存在大量未提交音频、工作台、测试与文档修改，已保留并纳入验证。Godot `4.7.1.stable.official.a13da4feb`，Apple M5 / Compatibility。
- **验证**：音频专项 209 项断言通过，覆盖双阵营三选一、重绑/晚到静音；工作台列举由完整回归覆盖。修正两个受新增部署声影响的旧夹具：缠流断言先排除部署事件；首击时序用例先结束前例短音，避免 headless 不等待播放完成造成预算干扰。完整自动验证结果见下方记录。
- **实际运行**：实例化正式 `scenes/main.tscn`，工作台生成双方两卡，每个变体各播放一次并录制 Master。六段来源均在运行录音中匹配，相关度 0.953–0.989；录音峰值盖伦 0.512、格温 0.338，无满幅削波。核对了两卡工作台实际截图。证据、录音、转录和复现脚本保存在 `builds/audio_review/2026-09-14_garen_gwen/`；自动转录仅辅助识别，不冒充人工听感。
- **查看方式**：F5 → 卡牌开发工作台 → 盖伦/格温 → 音频页筛选 `deploy:voice` 可逐个试听；实战页“放置当前卡”使用随机池。[盖伦音频](../units/audio/garen.md)、[格温音频](../units/audio/gwen.md)也提供三个素材链接。
- **遗留 / 未验证**：用户尚未确认最终听感；本轮未做新的发布包或长时间混音压力验收。未创建 Git 提交。

## 素材定位证据

| 用途 | 本地来源与核对 |
| --- | --- |
| 盖伦 · 人在塔在 | 客户端 `zh_CN-assets.wad` → `champion-choose-vo/86.ogg`，不是 Attack2DGeneral 的猜测映射 |
| 盖伦 · 勇往直前 / 保家卫国 | `Garen.zh_CN.wad.client`，Move2DStandard r16 / r11；中文转录对应台词（自动转录存在同音字误差），网站用于台词参照；本地与网站未匹配到相同波形，未宣称录音版本或字节相同 |
| 格温 · 英雄锁定音效 | 客户端 `default-assets2.wad` → `champion-sfx-audios/887.ogg`，不是通用选人 UI 事件 253；保留 2.6 秒、96 kHz 双声道，标准 PCM 文件头替换不改采样 |
| 格温 · 你要找裁缝吗 / 剪刀飞快 | `Gwen.zh_CN.wad.client`，Attack2DGeneral r17 / r20；与网站标注原声解码波形相关度分别 0.9983 / 0.9962 |

全部运行 WAV 来自只读外部素材库，网站只用于比对；未移动待开发队列内容。文件哈希、准确单变体媒体 ID、事件路径和加工步骤见两卡 `event_manifest.json`。旧错误候选保留为历史来源审计，不进入声音池。

## 最终自动验证

`python3 tools/dev.py verify --network --network-boundaries --output builds/verification/2026-09-14_deploy_cleanup` 全部通过：资源/文档审计、验证器夹具、Godot 导入、**3394 项机制断言**、差异检查、本地主客机终局，以及协议不匹配、内容不匹配、慢加载、断线、重开、建筑网络场景。各进程均完成结构化检查，无脚本或资源错误。

完整证据：[result.json](../../ClashLegends-开发素材库/04-中间产物/构建与验证/verification/2026-09-14_deploy_cleanup/result.json)。该证据记录当时含未提交修改的工作区；本节在验证结束后补写，之后只重查文档链接和文本差异，未再次改动游戏代码或音频。
