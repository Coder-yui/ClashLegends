# 交付合同：工程审查 D1 — 套件独立运行与隔离

- **需求 / 来源**：[工程审查 CL-09](../issues/2026-09-14_工程审查报告.md)，补齐 A 的可信执行与 B 的联网覆盖之后的隔离能力。
- **完成结果**：33 个套件统一注册于 suite_catalog.json；运行器支持列出、按名称选择和反序运行。每个套件使用固定种子和新场景，销毁后断言战斗对象和根节点没有遗留；无需依赖前序套件建立或恢复 Main 状态。统一执行器按当前选择核对完成清单，未知名称明确拒绝，选跑不能冒充全量通过。
- **验证**：基线 `5eab9a30fd4f380464fd1c111534db62387c6afe`，保留未提交修改；Godot `4.7.1.stable.official.a13da4feb`。独立场景[正序通过](../../builds/verification/20260914-133707-284675/result.json)；补充根节点清理断言后[反序 3101 项断言及 20 项工具测试通过](../../builds/verification/20260914-133820-309325/result.json)。ProjectileSuite、ActiveSkillSuite、network_lifecycle_suite、workbench_suite 分别在新 Godot 进程中[独立通过](../../builds/verification/20260914-independent-suites/result.json)，记录工作区前后摘要且一致。
- **查看方式**：`python3 tools/verify.py --suite ProjectileSuite --timeout 120`；完整命令及新增套件注册说明见[测试手册](../../tests/README.md)。
- **遗留 / 未验证**：资源缓存仍为进程共享，CardDB 内容依靠递归只读与隔离契约；本轮不强制每套件启动新进程。人工观感/听感不能由机制通过代替。CL-09 的执行可靠性、关键异常/联网路径及可独立运行要求已完成；CL-13 与 E 仍继续。
