# 交付合同：格温满层首次命中回血

- **需求 / 来源**：用户对话；满层快刀乱剪第一次命中时回血，未命中不回血。
- **完成结果**：满层首次有效命中立即回复 100 生命，经通用 `Unit.heal()` 显示回血特效；首剪落空后可在后续首次命中触发。同次施法多目标、多剪只回复一次，空剪、被目标交互保护拒绝和非满层不回复。命中前冰冻取消技能则无收益；命中后冰冻保留已回复生命。新增 `full_resource_first_hit_heal` 的读取、schema、数值校验与回归，同步卡牌说明及当前规则文档。
- **验证**：基线 `2ecbf51`，工作区含既有未提交修改并予以保留；Godot `4.7.1.stable.official.a13da4feb`。统一机制回归 7112 项断言全部通过，退出码 0；覆盖首剪命中、多目标去重、首剪落空后命中、全程空剪、目标保护、非满层和冰冻时序。项目审计 0 错误，`git diff --check` 通过。日志：本地开发素材库 `04-中间产物/预览与验证/格温首次命中回血-2026-09-26/mechanics.log`。
- **查看方式**：卡牌开发工作台选择格温的快刀乱剪，调整至满层且缺血，面对敌人施放；首次有效剪中即回血，空放不会回血。
- **遗留 / 未验证**：本轮仅修改权威回血时机，复用已有通用特效；未重复进行实际渲染目视验收和双端联机运行。
