# 技能提交与执行资格分离

- **需求 / 来源**：用户本轮明确将技能按钮和入队合并为提交资格；临时状态在实际执行时处理，纳尔变形允许先提交。
- **基线**：`b6bab2c4ebbfc915b2adc6ecaa0032344c21ba30`。开始时已有上轮先锋恢复边界的 Unit、回归及文档修改，全部保留；本轮未提交。引擎 Godot `4.7.1.stable.official.a13da4feb`。
- **完成结果**：Main `_can_submit_active_skill` 为按钮、玩家、AI、RPC共用提交规则，复用基础资格并检查费用和重复请求；`_active_skill_is_legal` 只负责执行时重新检查基础资格及 START_SKILL。ActiveSkillBar 通过回调获取可提交性，不再自行维护另一套禁用门禁。点击成功才进入等待，成功/拒绝回执解除等待；客户端成功回执补清 pending。临时眩晕、冰冻、变形、施法、击退和先锋阶段不禁用按钮；部署、费用、次数、冷却及已入队限制保留。
- **结算边界**：正式入队预扣费用，执行时已解锁则施放；仍存活但受限则原收据退款、次数不耗、无新冷却，不延期重试。执行时不再次以余额检查已付款请求，余额为零仍可正常施放。死亡/终局不退款、槽位替换退款、成功 Cast Start 才扣次数及之后冰冻不退款均保留。纳尔转换中的新请求不再入队前拒绝。
- **协议与文档**：规则算法变化，MatchSession 协议 42→43，既有六位权限及快照载荷不变；旧版本不混用。规则集中维护在 [ACTIONS](../status/ACTIONS.md)、[CORE](../status/CORE.md)、[IMPLEMENTATION](../status/IMPLEMENTATION.md)，历史交付保持原记录。
- **验证**：专项三套件 603 项通过；最终 48 套件、5283 项断言全部通过。配置审计、验证工具自检、导入、diff 检查通过；实际渲染双端主场景及 protocol/content/slow/slow_host/load_disconnect/load_timeout/disconnect/restart/buildings 共 10 组通过，验证期间工作区未变化。
- **查看方式**：部署有技能的单位后，在受控或转换期间点击技能；按钮进入等待，在原执行边界成功或退款。相关精确费用、次数、回执和状态矩阵见 StatusBoundarySuite、StructureRushSuite、ActiveSkillSuite、network_lifecycle_suite。
- **遗留 / 未验证**：人工观感、听感及跨公网验证未执行；本轮不改模型/动画/音频资源或效果生命周期。

## 本轮命令与证据

```sh
/Applications/Godot.app/Contents/MacOS/Godot --headless --path . --script tests/mechanics_check.gd -- --suite=StatusBoundarySuite --suite=network_lifecycle_suite --suite=StructureRushSuite
python3 tools/dev.py verify --godot /Applications/Godot.app/Contents/MacOS/Godot --network --network-boundaries --network-render --timeout 600 --output 'ClashLegends-开发素材库/04-中间产物/构建与验证/skill-submit-boundary-20260922'
```

[完整结果与日志](../../ClashLegends-开发素材库/04-中间产物/构建与验证/skill-submit-boundary-20260922/result.json)。同目录 `related-suites.log` 的 SUBMIT_BOUNDARY 记录状态、提前解除与否、实际次数、余额及施法序号。新增回归覆盖 6 类临时锁的解除/未解除、先锋准备/冲撞实际进入恢复后的拒绝、客户端快照基础资格及成功/拒绝回执；现有部署、死亡、替换、冷却、次数、费用与重复请求检查全量通过。费用夹具重置自然回复的残余计时，避免上个场景的金币回复混入退款断言。

已目视检查本轮 `host-status-32.png` / `client-status-32.png`：主机受控纳尔按钮可提交，客户端余额不足按钮禁用；渲染截图仅辅助观察，具体提交和结算以回归断言为准。未宣称人工听感或跨公网验收。

验证时完整工作区 SHA-256：`c6f6d05e5434db11dae28148fdc8283d2d823c0ea71f5422d38dcc880c9ee377`。验证后只补全本报告并再次执行 `git diff --check`；素材库证据按项目规则本地保存、不提交 Git。
