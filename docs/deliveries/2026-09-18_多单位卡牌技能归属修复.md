# 交付合同：多单位卡牌技能归属修复

- **需求 / 来源**：用户对话；七张多单位卡在部分成员死亡后，仍应给同一次下牌中实际生效时存活的成员施加技能。
- **完成结果**：修复工作台仅保存单个成员弱引用、该成员死亡后技能无法使用的问题。工作台独立保存部署编号，自动接替同批存活成员，全灭后不回退旧批次。所有部署成员保留下牌来源卡，避免共享近战兵等成员的不同卡牌混淆。正式技能资格也独立保存部署编号，引用失效时可恢复到同批存活成员；资格转交、工作台替补和效果执行共用存活成员查询。客户端先同步来源卡，再恢复主动资格。没有逐卡特判，后续使用 `target_scope: deployment_group` 的编队卡复用该机制。协议由36提升至37，载荷形状不变，联机双方需使用相同版本。
- **验证**：基线 `7e3caf5fd83b356387990714f470e40f06c0e9ac`；开始时工作区干净，交付为未提交修改。引擎 `4.7.1.stable.official.a13da4feb`。
  - `python3 tools/verify.py --network`：审计、验证器测试、导入、全量机制4491项断言、差异检查及双进程联网全部通过。日志：[全量结果](../../ClashLegends-开发素材库/04-中间产物/构建与验证/verification/20260918-215323-283987/result.json)。
  - 随后仅增加边界测试与说明文档；`python3 tools/verify.py --suite DeploymentSkillSuite --suite network_lifecycle_suite`：148项断言及配套检查通过。覆盖七卡工作台成员死亡并释放、其他批次/敌军隔离、全灭、正式请求期间连续转交、不重复付费与保留次数、共享成员的不同卡牌、延迟生效时死亡筛选、网络来源卡恢复。日志：[专项结果](../../ClashLegends-开发素材库/04-中间产物/构建与验证/verification/20260918-215537-518152/result.json)。
- **查看方式**：F5进入卡牌开发工作台，在实战页部署上述任意编队；部分成员死亡后点击释放技能，同批剩余成员仍能获得效果。正式比赛中的技能仍遵守原费用、次数和冷却。
- **遗留 / 未验证**：未改动美术或音频；本次未做人工画面、听感验收。已开始施法的施法者死亡取消其尚未发生效果，继续沿用既有时序规则。
