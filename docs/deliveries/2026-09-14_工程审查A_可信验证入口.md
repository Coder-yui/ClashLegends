# 交付合同：工程审查 A — 可信验证入口

- **需求 / 来源**：逐板块处理 [工程审查报告](../issues/2026-09-14_工程审查报告.md)。本轮完成 A 工作包（CL-09 的可信执行与证据归档部分），后续问题继续按 [处理状态](../issues/README.md) 推进。
- **完成结果**：新增 `python3 tools/dev.py verify`，复用已有审计和 Godot 机制入口，依次执行审计、维护工具自测、资源导入、机制回归与差异检查。每步独立日志、超时及进程组清理；失败即停止。机制入口在场景清理后输出唯一结构化结果，记录实际返回的套件，不固定历史断言数。执行器同时检查退出状态、错误日志、最终汇总和套件完成集合。归档含提交 SHA、脏工作区状态、文件内容 SHA-256、引擎版本、命令和耗时；运行期间工作区变化判失败。未修改战斗、数值或表现规则，保留进入任务前的工作台和文档改动。
- **验证**：基线 `5eab9a30fd4f380464fd1c111534db62387c6afe`，在含既有修改和本次修改的**脏工作区**执行；引擎 `4.7.1.stable.official.a13da4feb`。`python3 tools/dev.py verify` 全部通过：705 项机制断言、27 个套件/阶段完成记录、13 项维护工具测试，导入和机制日志无脚本或资源错误，`git diff --check` 通过。额外执行 `python3 -m unittest discover -s tools/tests`，5 项通过。故障夹具验证退出 0 但含脚本错误、缺失/重复汇总、套件遗漏/重复、断言失败、非零退出、超时与未提交文件变化都不会被当作成功。
- **证据**：本机 [结构化结果](/Users/czh/Projects/Clash%20Legends/builds/verification/20260914-110441-202282/result.json)、[机制日志](/Users/czh/Projects/Clash%20Legends/builds/verification/20260914-110441-202282/mechanics.log)、[维护工具测试日志](/Users/czh/Projects/Clash%20Legends/builds/verification/20260914-110441-202282/verifier-tests.log)。结果中的内容摘要是该次运行的身份；本报告及索引随后新增，不冒充同一内容版本。日志位于忽略的 `builds/`，清理构建目录前需自行保留要长期保存的证据。
- **查看方式**：按 [测试手册](../../tests/README.md) 从项目根目录运行 `python3 tools/dev.py verify`；结束时会打印新证据目录。直接运行 Godot 的旧入口仍可诊断问题，但统一执行器结果才包含完整判定和版本信息。
- **遗留 / 未验证**：CL-09 的独立领域筛选、可验证场景 reset、双进程联机自动化和乱序/终局集成覆盖尚未完成，随相关 B/C 板块推进；本轮未做联机、画面、音频或设备验收。CL-10 按现行约定使用交付文档，不恢复工作台验收持久化。其他审查条目尚未修复。回滚时仅撤销本报告列出的验证工具、编排结果输出及文档接入，避免覆盖原有工作台修改。
