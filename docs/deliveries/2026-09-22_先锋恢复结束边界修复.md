# 先锋恢复结束边界修复

- **需求 / 来源**：用户对话，仅修复撞后恢复多阻挡一个 Tick。
- **完成结果**：Unit.prepare_action_clocks 在请求/普攻资格判断前推进已有 RECOVERY；sim_tick 读取更新后的阶段，移除旧 recovering_rush 标记。运行代码仅改 Unit 的 8 行 diff；StructureRushState.tick 继续持有原恢复计时，Main 已有预处理顺序无需改动。未移动冲撞路径、碰撞、伤害，也未改动画、数值或付款政策。
- **验证**：基线 `b6bab2c4ebbfc915b2adc6ecaa0032344c21ba30`，开始工作区干净，当前修改未提交；Godot 4.7.1.stable.official.a13da4feb。相同先锋套件在原 Unit 上 390 项中 20 项失败，修复后 390 项通过。新增 126 项覆盖实际撞击后 K+12/K+13/K+14、眩晕/冰冻早于/同 Tick/晚于恢复结束、正式入队后的边界执行、次数费用及收据幂等。相关四套件 855 项通过；最终全量 48 套件、5200 项断言通过，审计、验证工具自检、导入及 diff 检查均通过，运行期间工作区未变化。
- **边界结果**：K 撞击后恢复仍为 0.65 秒，K+12 剩 0.05 秒且未行动；无控制/控制于 K+12 或 K+13 结束时普攻 K+13 起手；控制 K+14 结束时普攻 K+14 起手。合法排队技能 K+13 Cast Start，成功只扣一次费用/次数；K+13 仍受控则拒绝退款、不扣次数。恢复与控制独立，冻结取消动作不取消恢复锁，既有先锋动画/音频断言保留。
- **查看方式**：直接查看本文；精确合同维护在 [先锋特殊动作](../status/ACTIONS.md#先锋特殊动作)。
- **遗留 / 未验证**：本次只调整权威恢复时钟，不修改表现；未新增人工渲染、听感或双端验收，不沿用上轮结果作为本次结果。

## 本轮执行与证据

```sh
/Applications/Godot.app/Contents/MacOS/Godot --headless --path . --script tests/mechanics_check.gd -- --suite=StructureRushSuite --suite=StatusBoundarySuite --suite=ActiveSkillSuite --suite=AnimationStateSuite
python3 tools/dev.py verify --godot /Applications/Godot.app/Contents/MacOS/Godot --timeout 600 --output 'ClashLegends-开发素材库/04-中间产物/构建与验证/rush-recovery-boundary-20260922'
```

[完整验证结果](../../ClashLegends-开发素材库/04-中间产物/构建与验证/rush-recovery-boundary-20260922/result.json)，同目录 `baseline-rush.log`、`fixed-rush.log`、`related-suites.log` 记录修复前后准确行动 Tick。排队夹具通过正式 `use_active_skill` 入队并预扣真实费用，仅设置请求执行 Tick 模拟先入队后跨越冲撞的排程；撞击、恢复、执行与退款均走正式 Main 模拟链。

验证时工作区 SHA-256：`1ad517b1dfca5540670ff1d74640c3d2ac4f260263177d9811cc95585dabe088`。验证后仅补全本报告，并再次执行 `git diff --check`。
