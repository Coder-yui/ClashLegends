# 交付合同：工程审查 B2 — 实体生命周期同步

- **需求 / 来源**：[工程审查 CL-02](../issues/2026-09-14_工程审查报告.md)：生成、快照、销毁乱序时恢复一致实体集合，旧局消息不得污染新局。
- **完成结果**：新增独立 `NetworkEntityLifecycle`，持有会话标识、出生 Tick/生命周期序号、销毁标记和全量快照屏障，不依赖 Main 或场景树。协议升为 14，顶层含会话/生命周期序号，单位载荷 40 项并携带完整出生描述与当前部署剩余时间。快照可重建未知实体；删除仅对已覆盖该出生序号的快照生效，重复生成/销毁幂等，旧会话拒绝。额外覆盖同 Tick 不同生命周期序号，避免相同 Tick 的旧快照误删后生对象。重建描述使用当前主动资格，建筑保留塔墟和编队身份，清理统一释放导航和技能 UI。新局重置协议时钟。
- **验证**：基线 `5eab9a30fd4f380464fd1c111534db62387c6afe`，含既有修改的脏工作区；引擎 `4.7.1.stable.official.a13da4feb`。从基线提取原 Snapshot 实现并在现有等价创建路径上重放压缩数据，确认 Tick100 误删出生于 Tick101 的单位，Tick102 仍无法恢复；复现脚本按该反例退出 1。当前实现通过统一回归，新增生命周期套件实际往返序列化 Spawn 参数并应用压缩快照，覆盖先后乱序、丢包、重复消息、未知实体、旧局消息、新局 id 复用、建筑清理和同 Tick 竞态。随后运行真实 host/join 两进程约 15 秒，客户端接收超过 200 份快照、普通/兵线单位、弹体、双向形态和持续攻击端点；双方日志无脚本错误。
- **证据**：`builds/review-fixtures/cl02-baseline.log` 与同目录基线提取/复现脚本；`builds/verification/20260914-114501-024189/` 保存修复后统一验证的结构化结果、内容摘要和日志；`builds/review-fixtures/b2-network/host.log`、`join.log` 为真实双进程冒烟。进程已终止清理。
- **查看方式**：`python3 tools/dev.py verify` 会自动运行新增协议回归。日常联机进入同一版本的主机与客户端。
- **遗留 / 未验证**：尚未验证 WAN/真实网络延迟和目标移动设备；两进程冒烟不等于终态一致性验收。会话身份、握手及所有表现 RPC 的会话门禁在 B4 收敛，终局可靠状态在 B3 处理。全量描述增加的带宽由 E 板块量化，不预先声称性能提升。协议双方必须同时升级；回滚需同步恢复 Main RPC、快照格式、生命周期类、协议测试和文档，保留既有工作台修改。
