# 交付合同：协作流程与工作台简化

- **需求 / 来源**：用户对话及 [工程审查报告](../issues/2026-09-14_工程审查报告.md) CL-10。按用户要求替代原建议，建立简洁交付流程。
- **完成结果**：新增 `docs/issues/` 问题档案、`docs/deliveries/` 任务交付和 [统一模板](../templates/delivery.md)，写入 AGENTS 与任务导航。原审查报告已从 Downloads 移动，保留原文；采纳调整和逐板块待办写在问题索引。删除工作台验收页、勾选、备注和 Markdown 导出，仅保留模型、实战、音频预览；同步文档、测试和演示脚本。未改变战斗规则。
- **验证**：基线 `5eab9a3`，任务开始工作区干净，验证时包含本任务未提交修改；Godot `4.7.1.stable.official.a13da4feb`。核心回归 705 项断言全部通过；项目审计 24 张卡、2387 个文件、0 错误，`git diff --check` 通过；实际渲染演示正常退出，切页暂停/恢复检查通过，两份最终日志无 SCRIPT ERROR / ERROR。Agent 已目视模型页，三个标签与预览布局正常；不代表用户已确认，也未声称本轮音频听感验收。
- **查看方式**：F5 → 卡牌开发工作台，可看到三个预览页。以后直接提需求即可，Agent 完成后填写交付报告并给链接；不需要在工作台标记验证状态。
- **遗留 / 未验证**：本次完成协作流程与 CL-10 替代方案，其他审查板块仍待处理，下一板块为 CL-09 可信验证入口。未进行双进程联网和目标设备测试。历史 `user://workbench_reviews/` 导出文件未删除。

验证命令：

```sh
Godot --headless --path . --script tests/mechanics_check.gd
Godot --path . --script tools/demos/workbench_preview.gd
python3 tools/maintenance/audit_project.py
git diff --check
```

本机临时日志：`/tmp/clash-workbench-mechanics.log`、`/tmp/clash-workbench-preview.log`、`/tmp/clash-workbench-audit.log`；目视截图：`/tmp/clash-workbench-model.png`。临时文件不作为永久归档凭证。
