# 交付合同：卡牌开发工作台重整

- **需求 / 来源**：用户对话。替换难以扩展的选牌下拉列表，支持 0–8 张实验卡快速切换，消除实战面板遮挡；用户确认独立宽屏、双方共用快捷栏、可重复放置且自动保存，随后要求放大地图、以开发观察为优先。
- **完成结果**：左侧牌库、快捷栏和工具，右侧大模型预览 / 实战；牌库卡面网格支持名称和 ID 搜索、类型与衍生形态筛选。快捷栏最多八张、可为空、数字键 1–8 切换、自动保存，调整牌组或阵营不清场。实战默认 140% 放大，滚轮 100%–300% 缩放、右键 / 中键拖动、一键全图、暂停 / 继续；退出恢复原窗口。工具不覆盖地图。权威战场仍为 720×1280，出牌沿用原正式入口。
- **验证**：基线 `4ca4f25`，工作区原有大量未提交修改，验证针对当前工作区；Godot `4.7.1.stable.official.a13da4feb`。核心入口完整通过 6531 项断言、无脚本或资源错误；工作台包含搜索、八槽与空栏、共享阵营、快捷键、持久化、镜头边界和退出恢复回归。项目审计零错误，`git diff --check` 通过。实际渲染复用 `tools/demos/workbench_preview.gd`，已目视检查放大 / 全图战场、牌库与模型布局，脚本检查切页暂停、返回继续和退出恢复。
- **查看方式**：F5 → 卡牌开发工作台 → 打开牌库 → 加入快捷栏 → 实战·技能。详见 [使用说明](../DEVELOPMENT_WORKBENCH.md)。最新渲染证据在本机素材库 `04-中间产物/预览与验证/1790148841-15328/`，包含 `clash-workbench-battle.png` 与 `clash-workbench-battle_overview.png`。
- **遗留 / 未验证**：用户尚未确认最终交互观感；默认放大观察会裁切地图两端，可拖动或一键全图查看。未进行跨操作系统窗口与输入设备验证；本轮未改变音频规则，未新增听感验收。
