# 状态机制执行边界修复

- **需求 / 来源**：用户本轮七项边界修复要求；延续[既有状态改造](../issues/2026-09-22_既有状态效果规则改造与验收.md)。仅修复已有机制，不增加延期状态。
- **基线 / 环境**：`3463f2e1d1d0105e4abbcf8afae8d09cc576ce68`，开始时工作区干净；本轮改动未提交。Godot `4.7.1.stable.official.a13da4feb`，macOS Apple M5，实际渲染使用 OpenGL Compatibility。
- **完成结果**：六类边界均复现并修复，原正确的付款收据与 Cast Start 扣次数逻辑保留。固定 20Hz、阶段内同时结算、普通 Unit、独立弹体/星辰/区域及先锋既有恢复方案保持。规则集中更新于 [CORE](../status/CORE.md)、[ACTIONS](../status/ACTIONS.md)、[PRESENTATION](../status/PRESENTATION.md)、[IMPLEMENTATION](../status/IMPLEMENTATION.md)；规则页统一标明目标规则与已实现子集。

## 复现、修改与边界结果

以当前新增回归运行基线生产脚本的隔离副本，76 项断言出现 17 项失败，无脚本错误；没有回退或覆盖当前工作区。基线日志见下方证据目录 `baseline-3463f2e-76-checks.log`。最终专项新增区域边界与幂等退款核对，共 78 项通过。

| 问题 | 复现与具体修改入口 | 预期与本轮实际结果 |
| --- | --- | --- |
| 纳尔解冻先后 | 已复现。`Main._sim_step` 在命令前推进旧状态/动作时钟并兑现 `Unit._apply_pending_form`；清理死亡及失效代次标记 | 提前入队、冰冻中第六枚真实弹体命中、同 Tick 解冻/执行：先进入真实转换，再拒绝请求，原收据退款一次、次数不变、无主动拍地/冷却；重复结算在非资源上限场景仍不重复退款。转换中间拒绝、结束边界重判；合法主动变大不自我拒绝。主客端资格/次数收敛 |
| 凤凰蛋生命周期 | 已复现创建顺序改变攻击计数。`Unit.prepare_natural_lifecycle` 接管定时复生；`_die` 立即退休；Main 固定参与名单；CombatResolver 保留批次合法资格并提供实际送达回调，Unit 回撤最终未送达近战段号及依附追加刀 | 交换蛋/攻击者创建顺序，孵化、目标、实际伤害、回执和计数一致；边界前一 Tick 可命中旧蛋，孵化边界旧蛋无资格，新凤凰不行动、不继承控制；前阶段致死不孵化、旧弹体不转移；致盲正式出手例外保留 |
| 施法统一计时 | 已复现创建 Tick 提前扣锁。`Unit.prepare_action_clocks` 移到命令前，与 CommandSchedule 旧记录推进对齐；Effect/SpellSystem 标记区域创建 Tick；Main 保存本次 `cast_forward` | K 开始的一秒锁在 K+20 到期；零延迟即时。正式命令路径记录格温 Impact 相对 Tick `[2,13,16,19,21]`，完成回血/解锁 `30`；盖伦 `[20,40,60]`、解锁 `60`；龙王独立星辰 `23`、锁结束 `39`。眩晕保持同一时间线，冰冻取消后段/完成收益，已创建星辰继续。延迟区到期边界立即生效且不消耗新区域首 Tick。没有改技能配置或普攻小数节奏 |
| 首次渲染前眩晕 | 已复现。`UnitModel3D._sync_control_override/_sync_visual` 在 Idle 回退前消费仍有效的权威保护动作；继续过滤取消序号、代次、已结束动作 | 未先更新模型即施加眩晕，技能/纳尔合法转换按权威经过时间播放；客户端首次快照同时带动作与眩晕也播放；交错冰冻取消与旧快照不恢复旧动作/声音；空动作正确清理先锋旧准备序列 |
| 身体朝向 | 已复现基线冻结后朝向从 RIGHT 回退 UP、根 yaw 从 2.953097 继续到 3.066194。`Unit.get_visual_facing_direction` 保存身体方向，控制入口先捕获；`UnitModel3D._sync_transform` 受控停止 yaw 插值；先锋合法起手保存方向 | 攻击方向明显不同于旧移动方向时，冻结保持当前显示 yaw；击退仅平移。眩晕保护技能仍播骨骼且无自主转向。身体方向与保存的伤害方向分开；主客机覆盖 |
| 客户端技能资格 | 已复现未推进本地计时器导致误判。`Unit.action_permissions` 客户端读取完整快照权限；Main 请求与按钮共用 START_SKILL；NetworkSnapshotSystem 校验六位掩码，MatchSession 协议升级 42 | 部署/转换/施法/冻/晕/先锋冲撞恢复均读取权威权限；本地过期计时器不能拒绝已合法快照，也不能提前解除锁。迟到快照保持最后有效投影，主机执行重判及退款仍生效；旧双位消费者显式掩码，先锋回归通过；旧协议和未知位拒绝 |

## 本轮验证

最终固定工作区验证 **PASS**：全量机制 **5074 项断言、48 个套件全部通过**；配置/资源审计 **34 张卡牌、0 错误**；验证工具自检 **20 项通过**；导入及 `git diff --check` 通过；真实渲染双端主场景及 protocol/content/slow/slow_host/load_disconnect/load_timeout/disconnect/restart/buildings 共 **10 组通过**。验证器确认运行期间工作区未变化。

[本轮完整结果与日志](../../ClashLegends-开发素材库/04-中间产物/构建与验证/status-boundary-stable-20260922/result.json)；验证时完整工作区指纹为 `5673bd432dfe7f2e8f384b228b01e20ef77819574df920fbde7de137b1f89364`。

主要执行命令：

```sh
Godot --headless --path . --script tests/mechanics_check.gd -- --suite=StatusBoundarySuite
Godot --path . --audio-driver Dummy --script tests/mechanics_check.gd -- --suite=AUDIO_PRESENTATION_SUITE_SCRIPT
Godot --path . --audio-driver Dummy --script tools/demos/maintenance_preview.gd -- --status-boundaries --cards=sett,gnar,gwen,aurelionsol,garen,anivia_egg
python3 tools/dev.py verify --godot /Applications/Godot.app/Contents/MacOS/Godot --network --network-boundaries --network-render --timeout 600 --output 'ClashLegends-开发素材库/04-中间产物/构建与验证/status-boundary-stable-20260922'
```

`Godot` 实际使用上述应用中的绝对可执行文件。先运行相关套件，再执行统一全量。早期失败包括旧的一 Tick 预期、旧权限双位等号、批次中人工推进生命周期的测试、先锋空动作清理，均修正后验证。后台窗口 `frame_post_draw` 截图等待会挂起双端测试，测试脚本改为 `RenderingServer.force_draw(false)` 后真实双端通过。前一轮所有阶段通过但运行期间补充区域修复，验证器正确报工作区变化；不将该轮计作成功交付。

### 实际渲染与声音证据

[预览与自动声音证据](../../ClashLegends-开发素材库/04-中间产物/预览与验证/1790054521-83607/status-boundaries)：84 张 PNG、12 份 WAV 及 `signal-check.json`。目视检查首次施法即眩晕、冻住的瑟提姿势/yaw与平移、纳尔形态、格温、盖伦、龙王星辰、蛋到凤凰；实际双端截图亦已检查。渲染运行中的声音套件 **217 项通过**，包括取消、归属、独立事件；12 份录音均为非零信号。录音非零仅证明采集正常，不能替代归属/中断断言。

上述证据为本地素材库内容，按项目规则忽略 Git；正式代码、测试及文档位于版本工作区。新回归已注册到 `tests/suite_catalog.json` 和统一验证入口，没有额外平行验证系统。

- **代码指纹**：`scripts/`、`tests/`、`tools/` 的 468 个文件，排除 `__pycache__`/`.pyc`，按相对路径排序累加 `路径 UTF-8 + NUL + 文件字节 + NUL` 后 SHA-256 为 `490082039a65c62fabe32caa0c866309b578f07ec2a601e0baf9570e9b2aa5bd`。全量运行的完整工作区指纹见结果 JSON；验证后仅补全文档报告。
- **查看方式**：直接查看本文及规则页；F5 进入卡牌开发工作台，或执行上述实际渲染预览命令。
- **遗留 / 未验证**：没有未完成的代码修复项。未进行人工听感确认、用户观感确认、跨公网/丢包压力或异构平台联机；本轮双端为同机真实 ENet 进程。高实体密度测试出现 MTU 包大小警告，双端结果收敛且没有脚本错误，本轮未调整协议打包策略。延期状态继续维持 IMPLEMENTATION 中的范围。
