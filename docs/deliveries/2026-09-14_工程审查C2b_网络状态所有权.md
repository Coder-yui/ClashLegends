# 交付合同：工程审查 C2b — 网络状态所有权

- **需求 / 来源**：[工程审查 CL-06](../issues/2026-09-14_工程审查报告.md)，承接音频所有权修复。
- **完成结果**：移除 Main 的主机/客户端弹体字典别名，ProjectileSystem 持有快照目标、插值位置及清场入口，外部读取副本。NetworkSnapshotSystem 显式接收弹体系统，通过 Main 的实体与技能注册入口、比赛元数据入口更新副本；MatchRules 持有时间及结束状态。Tower 统一应用副本、触发死亡表现并释放导航。法术 RPC 与权威结算共用所属系统的表现创建入口；定向技能 RPC 不再直接追加效果集合。RPC 仍在 Main Node，载荷和协议版本保持不变。
- **验证**：基线 `5eab9a30fd4f380464fd1c111534db62387c6afe`，含既有及本轮未提交修改；Godot `4.7.1.stable.official.a13da4feb`。3002 项机制断言、18 项工具测试、审计、导入、diff 检查及双进程终态一致性通过，见[结果](../../builds/verification/20260914-133445-694278/result.json)。新增输入/输出嵌套字典隔离、连续目标插值保持、缺席弹体删除与跨局清场测试。过程中发现测试旧构造参数，修复后完整重跑；不采用失败运行作为证据。
- **查看方式**：运行 `python3 tools/verify.py --network --timeout 60`；本轮没有玩家操作变化。
- **遗留 / 未验证**：CL-06 已完成本轮真实所有权问题收敛；Main 保留实体装配和局面编排，网络仍读取实体状态并保留自动联机诊断标志，不以消除所有下划线访问作为目标。弹体表现副本成本纳入 E 性能测量。CL-09 独立套件与 D/E 继续处理，整体工程审查尚未完成。
