# 艾希（`ashe`）

## 1. 属性数据

| 属性 | 数值 |
| --- | --- |
| 类型 / 费用 / 可选 | 单位 / 3 / 是 |
| 描述 | 远程射手，能攻击空中和地面目标，在安全距离持续输出。 |
| 生命值 / 普攻伤害 | 340 / 58 |
| 攻击距离 | 170 |
| 移速 | 52 px/s（稍慢） |
| 攻击间隔 / 首次命中 | 1.00 s / 0.45 s |
| 体型 / 权威半径 / 表现半径 | 中 / 18 / 22.5 |
| 质量 / 视野 | 3 / 240 |
| 空中单位 / 仅攻击建筑 / 可攻击空中 | 否 / 否 / 是 |
| 部署时间 | 1.0 s（默认值） |
| 弹体 | 480 px/s，`arrow`；表现高度 45（`30 × 1.5`） |
| 溅射 / 击退 | 0 / 0 |
| 阵营颜色 | `Color(0.50, 0.85, 0.95)` |

## 2. 被动

无独立被动字段。艾希的基础攻击通过箭形弹体攻击地面和空中目标。

## 3. 主动技能

**万箭齐发**（`kind = frontal`，`shape = fan`）

- 主动技能规则：消耗 `2` 金币；每个艾希最多使用 `1` 次；每次使用后冷却 `10.0 s`。
- 锁定施法开始时的朝向，在前方长度 `190`、夹角 `72°` 的环形扇区进行结算；内边界沿艾希体型外围的前半圆弧，侧边从人物左右两侧延伸到远端圆弧。
- 表现上射出 `8` 根箭矢；权威命中对每个符合范围的目标造成 `70` 点伤害。
- 命中单位减速 `1.0 s`，速度倍率为 `0.55`（保留 55% 移速）。
- `impact_delay = 0.62 s`，`cast_duration = 1.833333 s`；施法期间锁定移动、普通攻击和朝向。

## 4. 使用的动画

- 场景：`res://assets/units/ashe/ashe_view.tscn`；`visual_forward_yaw = 0`。
- 部署 / 待机：`Idle1` / `Idle1`。
- 移动：`Run`。
- 普攻：`Attack1`、`Attack2`。
- 主动技能 `active`：`Spell2`，动画时长 `1.0 s`，淡入 `0.05 s`、淡出 `0.10 s`；权威施法窗口仍为 `1.833333 s`。
- 死亡：`Death`，表现时长 `0.8 s`。
