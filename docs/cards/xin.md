# 赵信（`xin`）

## 1. 属性数据

| 属性 | 数值 |
| --- | --- |
| 类型 / 费用 / 可选 | 单位 / 4 / 是 |
| 描述 | 近战战士，部署时发动新月护卫伤害并击退周围地面敌人，连续攻击还能恢复生命。 |
| 生命值 / 普攻伤害 | 620 / 68 |
| 攻击距离 | 40 |
| 移速 | 68 px/s（稍快） |
| 攻击间隔 / 首次命中 | 0.90 s / 0.30 s |
| 体型 / 权威半径 / 表现半径 | 中 / 18 / 22.5 |
| 质量 / 视野 | 5 / 220 |
| 空中单位 / 仅攻击建筑 / 可攻击空中 | 否 / 否 / 否 |
| 部署时间 | 1.0 s |
| 弹体 / 溅射 / 击退 | 无 / 0 / 0 |
| 阵营颜色 | `Color(0.85, 0.30, 0.25)` |

## 2. 被动

### 新月护卫（部署）

- 生成当帧以自身为中心结算一次，半径 `90`，造成 `90` 点伤害；基础击退距离 `90`，位移持续 `0.25 s`。
- 只影响敌方地面战斗对象。地面建筑与防御塔会受伤但不会被击退，空中单位完全不受影响。
- 实际击退距离乘以 `clamp(4 / target_mass, 0.35, 1.0)`；轻单位倍率封顶 `1.0`，不会再明显超过基础击退距离。

### 无畏战吼（命中回血）

- 每 3 次真正命中的普攻回复 `60` 点生命值。
- 回复不会超过最大生命值；未命中、被拦截或没有造成有效伤害的攻击不触发。

## 3. 主动技能

**新月护卫**（`kind = nova`）

- 主动技能规则：消耗 `1` 金币；每个赵信最多使用 `2` 次；每次使用后冷却 `8.0 s`。
- 与部署版使用同一组权威参数：半径 `90`、伤害 `90`、基础击退距离 `90`、位移持续 `0.25 s`。
- 只影响地面目标；建筑与防御塔可受伤但不会被击退，空中单位完全不受影响。
- 击退同样使用 `clamp(4 / target_mass, 0.35, 1.0)` 的质量倍率。
- Gameplay Impact 在施放开始时结算；Spell4 的 1 秒施法窗口锁定移动、攻击与朝向。

## 4. 使用的动画

- 场景：`res://assets/units/xin/xin_view.tscn`；`visual_forward_yaw = 0`。
- 部署新月护卫：只播放 `Spell4`（`1.0 s`）；部署后移动接 `Spell4_To_Run`，部署后攻击使用统一 Pose 衔接。
- 待机 / 移动：`IdleBase` / `RunBase`。
- 三段普攻出手：`Attack1_Hit`、`Attack3_Hit`、`Passive_AA_01_XinZhaoRework_anm`；第三段使用完整的 `Passive_AA_01_XinZhaoRework_anm`。
- 第一、二段普攻命中后的收势：`Attack_AA_01_settle_XinZhaoRework_anm`、`Attack_AA_03_settle_XinZhaoRework_anm`；第三段没有额外收势片段。
- 第一、二段普攻后移动：`RunIn`；第三段被动攻击后移动：`PassiveAA_to_Run_XinZhaoRework_anm`。
- 主动新月护卫：`Spell4`（`1.0 s`）；主动后移动接 `Spell4_To_Run`，主动后攻击使用统一 Pose 衔接。
- 死亡：`Death`，表现时长 `0.8 s`。
