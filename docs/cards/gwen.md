# 格温（`gwen`）

## 1. 属性数据

| 属性 | 数值 |
| --- | --- |
| 类型 / 费用 / 可选 | 单位 / 4 / 是 |
| 描述 | 近战刺客，首次普攻命中后进入缠流，能避开远处敌人的视野和锁定。 |
| 生命值 / 普攻伤害 / 攻击距离 | 580 / 62 / 30 |
| 移速 | 76 px/s（快） |
| 攻击间隔 / 首次命中 | 0.85 s / 0.28 s |
| 体型 / 权威半径 / 表现半径 | 中 / 18 / 22.5 |
| 质量 / 视野 | 4 / 210 |
| 空中单位 / 仅攻击建筑 / 可攻击空中 | 否 / 否 / 否 |
| 部署时间 | 1.0 s（默认值） |
| 技能资源 | 最大 3 层；每次真正命中获得 1 层 |
| 弹体 / 溅射 / 击退 | 无 / 0 / 0 |
| 阵营颜色 | `Color(0.95, 0.75, 0.85)` |

## 2. 被动

### 丝缕缠流

- 第一次真正的普通攻击命中后开启，影响半径 `120`。
- 开启后，距离格温超过 `120` 的敌方单位/塔不能将她作为目标，也不能对她造成伤害；敌方仍可看到她，圈内攻击正常生效。
- 赵信的击退等强制位移会关闭缠流；资源条只有主动槽实际携带本技能时才启用。

## 3. 主动技能

**快刀乱剪**（`kind = frontal`，`shape = fan`）

- 普攻命中充能，最多 `3` 层；Cast Start 固化当前层数并清空资源，施法期间获得的新资源留给下一次技能。
- 前方长度 `150`、夹角 `70°` 的扇形剪切，中央区域比例 `0.36`，中央伤害倍率 `1.2`；`ground_only = true`。
- 伤害按层数为：0 层 `40`、1 层 `80`、2 层 `120`、3 层 `160`；中央区域再乘 `1.2`，满层中央为 `192`。
- 普通资源：`impact_delay = 1.82 s`、`cast_duration = 2.6333335 s`；满层：`impact_delay = 1.98 s`、`cast_duration = 2.8000002 s`。
- 施法期间锁定移动、普通攻击和朝向；没有弹体（`projectile_count = 0`）。

## 4. 使用的动画

- 场景：`res://assets/units/gwen/gwen_view.tscn`；`visual_forward_yaw = 0`。
- 部署 / 待机 / 移动 / 进入移动：`Respawn` / `Idle_anm` / `Run_anm` / `Into_Run`。
- 普攻：`Attack1`、`Attack2`、`Attack3`。
- 普通资源主动技能 `active`：`Spell1_0`（`1.6666664 s`）→ `Spell1_C_anm`（`0.9666671 s`）。
- 满资源主动技能 `active_strong`：`Spell1_0`（`1.6666664 s`）→ `Spell1_B`（`0.1666667 s`）→ `Spell1_C_anm`（`0.9666671 s`）。
- 技能转移动画：`Spell1_C_to_Run_anm`。
- 死亡：`Death`，表现时长 `0.8 s`。

